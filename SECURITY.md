# 安全与公开发布边界

## 仓库定位

这是一个准备公开发布的 Agent Skill 项目。仓库只应包含可复用的 Skill 指令、必要参考文件和经过筛选的演示案例，不应包含个人生产环境或私有业务数据。

## 禁止提交

以下内容不得进入仓库：

- GitHub、OpenAI、飞书、微信或其他平台的令牌和密钥；
- Cookie、浏览器 Profile、会话状态和账号导出；
- `.env`、数据库、证书、私钥和签名文件；
- 用户原始脚本、未脱敏业务资料和临时分享链接；
- uTools 数据库或完整笔记存储；
- 剪辑工程、原始录屏、机器缓存、日志和临时目录；
- 未经确认可以公开分发的第三方素材。

## 示例图片

`cases/` 只保存用于说明 Skill 输出效果的精选静态图片。维护和新增图片时必须确认：

1. 页面不含账号、密钥、真实业务资料或临时链接。
2. 图片由维护者制作，或已获得公开展示所需的授权。
3. 产品名称、商标和界面截图仅用于说明相关教程场景，不暗示官方背书。
4. 图片已完成实际渲染和人工检查。
5. `cases/CASE_INDEX.md` 已记录来源、尺寸、用途和校验值。

示例图片不应被当作第三方品牌素材包或官方模板重新分发。

## 开源许可证

代码、Skill 指令、HTML 母版、验证脚本和文档采用根目录 [MIT License](LICENSE)。

`cases/` 下的 PNG 图片明确排除在 MIT 授权之外，适用 [cases/LICENSE.md](cases/LICENSE.md) 的保留版权条款。新增或替换案例时必须继续确认图片来源、公开展示权、第三方商标边界和个人信息脱敏。

## 提交前检查

```bash
git diff --cached --check
git ls-files
git grep --cached -n -I -E 'api[_-]?key|access[_-]?token|client[_-]?secret|BEGIN .*PRIVATE KEY|password'
```

如果安装了 gitleaks，再执行完整扫描。若未安装，应明确说明只完成了替代性的路径、文件类型和关键词检查。

## 发现问题

如果发现敏感信息：

1. 立即停止推送或公开发布。
2. 确认敏感内容是否已经进入 Git 历史。
3. 已推送的凭证应先撤销或轮换。
4. 清理 Git 历史后重新执行扫描。
5. 回读远端文件树和 README，确认问题已经消失。
